#pragma once

// Sets up program resources
void ProgramSetup();

// Resets the program data when opening a new PCK
void ResetProgramData();

// Handles program keyboard/button input
void HandleInput();

// Handles the main file tree
void HandleFileTree();

// Handles the menu bar
void HandleMenuBar();